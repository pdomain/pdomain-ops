# Verbatim research evidence, kept exactly as produced. No hook may
# reformat, fix, or lint it.
exclude: '^docs/research/evidence/'

default_install_hook_types: [pre-commit, commit-msg]

repos:
-   repo: https://gitlab.com/vojko.pribudic.foss/pre-commit-update
    rev: v0.9.0
    hooks:
    # Manual stage only. This hook rewrites the pinned `rev:` values in this
    # file and then fails the run because it modified a file, so every
    # `git commit` and CI run fails whenever a hook upstream publishes a
    # release. Bumping pins is a maintenance action, not a gate. Run it with
    # `make update-hooks`.
    -   id: pre-commit-update
        stages: [manual]
-   repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
    -   id: trailing-whitespace
    -   id: end-of-file-fixer
    -   id: check-yaml
    -   id: check-json
    -   id: check-toml
    -   id: check-added-large-files
        args: [--maxkb=1000]
    -   id: debug-statements      # catches leftover pdb/ipdb/breakpoint()
    -   id: check-merge-conflict
-   repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
    -   id: gitleaks
-   repo: https://github.com/astral-sh/ruff-pre-commit
  # Ruff version.
    rev: v0.16.7
    hooks:
    # Run the linter for imports
    -   id: ruff-check
        args: [--select, I, --fix]
    # Automatically fix issues.
    -   id: ruff-check
        args: [--fix]
    # Run the formatter.
    -   id: ruff-format
-   repo: https://github.com/DavidAnson/markdownlint-cli2
    rev: v0.23.2
    hooks:
    -   id: markdownlint-cli2
    -   id: markdownlint-cli2
        alias: markdownlint-cli2-fix
        args: [--fix]
        stages: [manual]
-   repo: local
    hooks:
    -   id: uv-lock-check
        name: uv.lock is in sync with pyproject.toml
        entry: uv lock --check
        language: system
        stages: [pre-commit]
        pass_filenames: false
        files: ^(pyproject\.toml|uv\.lock)$

    -   id: basedpyright
        name: basedpyright type check (strict on package; workspace canonical)
        entry: uv run basedpyright pdomain_ops --level error
        language: system
        stages: [pre-commit]
        pass_filenames: false
        files: ^pdomain_ops/.*\.py$
-   repo: https://github.com/jorisroovers/gitlint
    rev: v0.19.1
    hooks:
    -   id: gitlint
        stages: [commit-msg]
